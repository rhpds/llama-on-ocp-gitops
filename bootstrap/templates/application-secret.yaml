{{ if .Values.slack_mcp_server_secrets -}}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secret
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: {{ .Values.git.url }}
    targetRevision: {{ .Values.git.revision }}
    path: {{ .Values.slack_mcp_server_secrets.path }}
    helm:
      values: |
        namespace: {{ .Values.slack_mcp_server_secrets.namespace}}
        name: {{ .Values.slack_mcp_server_secrets.name }}
        path: {{ .Values.slack_mcp_server_secrets.path }}
        slack-bot-token: {{ .Values.slack_mcp_server_secrets.slack-bot-token }}
        slack-team-id: {{ .Values.slack_mcp_server_secrets.slack-team-id }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.slack_mcp_server_secrets.namespace }}
  syncPolicy:
    automated: {}
{{- end }}
